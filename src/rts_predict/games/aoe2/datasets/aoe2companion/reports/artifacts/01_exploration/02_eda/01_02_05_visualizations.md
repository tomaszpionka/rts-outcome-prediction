# Step 01_02_05 -- Univariate Census Visualizations: aoe2companion

**Generated by:** `sandbox/aoe2/aoe2companion/01_exploration/02_eda/01_02_05_visualizations.py`
**Source artifact:** `01_02_04_univariate_census.json`
**Plots directory:** `artifacts/01_exploration/02_eda/plots/`

## Plots

### Plot 1: won Distribution Bar Chart

**File:** `plots/01_02_05_won_distribution.png`

**Caption:** Vertical bar chart showing TRUE/FALSE/NULL counts for matches_raw.won; reveals ~4.69% NULL rate on the prediction target.

### Plot 2: Intra-Match won Consistency Stacked Bar

**File:** `plots/01_02_05_won_consistency.png`

**Caption:** Stacked bar showing intra-match consistency categories for 2-player matches; ~10.98% of matches have both_true or both_false labels.

### Plot 3: matches_raw.leaderboard Distribution

**File:** `plots/01_02_05_leaderboard_distribution.png`

**Caption:** Horizontal bar chart of leaderboard values in matches_raw.

### Plot 4: matches_raw.civ Top 30

**File:** `plots/01_02_05_civ_top30.png`

**Caption:** Horizontal bar chart of top-30 civilization values in matches_raw.

### Plot 5: matches_raw.map Top 30

**File:** `plots/01_02_05_map_top30.png`

**Caption:** Horizontal bar chart of top-30 map values in matches_raw.

### Plot 6: matches_raw.rating Histogram

**File:** `plots/01_02_05_rating_histogram.png`

**Caption:** Histogram with bin_width=100 showing rating distribution; skew=0.5662, kurt=1.6157.

### Plot 7: matches_raw.ratingDiff Histogram

**File:** `plots/01_02_05_ratingDiff_histogram.png`

**Caption:** Histogram with bin_width=10 showing ratingDiff distribution; skew=0.1105, kurt=0.8900.

### Plot 8: leaderboards_raw Numeric Boxplots (two-panel)

**File:** `plots/01_02_05_leaderboards_boxplots.png`

**Caption:** Two-panel boxplots (rating-scale and activity-count with symlog scale); season and rankLevel excluded as constants.

### Plot 9: matches_raw Completeness Matrix (55 columns)

**File:** `plots/01_02_05_completeness_matrix.png`

**Caption:** Horizontal bar chart of NULL % for all 55 matches_raw columns, color-coded green/orange/red by severity.

### Plot 10: profiles_raw NULL Rates

**File:** `plots/01_02_05_profiles_null_rates.png`

**Caption:** Horizontal bar chart distinguishing 100% NULL (dead) columns from live columns in profiles_raw.

### Plot 11: leaderboards_raw.leaderboard Distribution

**File:** `plots/01_02_05_lb_leaderboard_distribution.png`

**Caption:** Horizontal bar chart of leaderboard type counts in leaderboards_raw.

### Plot 12: matches_raw Boolean Column Proportions

**File:** `plots/01_02_05_boolean_stacked.png`

**Caption:** Stacked bar chart showing TRUE/FALSE/NULL proportions for all 18 boolean columns.

### Plot 13: Monthly Match Volume

**File:** `plots/01_02_05_monthly_volume.png`

**Caption:** Dual-axis line chart of monthly distinct match count and total rows; vertical reference line marks ratings_raw start (2022-09-10).

## SQL Queries (Invariant #6 -- Reproducibility)

All DuckDB SQL queries used to produce plots in this notebook are listed verbatim below.
Any plot produced from a DuckDB query can be reproduced by running the query against
the persistent `aoe2companion` DuckDB.

### rating histogram (Plot 6)

```sql
SELECT (FLOOR(rating / 100) * 100)::INTEGER AS bin, COUNT(*) AS cnt
FROM matches_raw WHERE rating IS NOT NULL
GROUP BY bin ORDER BY bin
```

### ratingDiff histogram (Plot 7)

```sql
SELECT (FLOOR("ratingDiff" / 10) * 10)::INTEGER AS bin, COUNT(*) AS cnt
FROM matches_raw WHERE "ratingDiff" IS NOT NULL
GROUP BY bin ORDER BY bin
```

### monthly match volume (Plot 13)

```sql
SELECT
    DATE_TRUNC('month', started) AS month,
    COUNT(DISTINCT matchId) AS distinct_matches,
    COUNT(*) AS total_rows
FROM matches_raw
WHERE started IS NOT NULL
GROUP BY month
ORDER BY month
```

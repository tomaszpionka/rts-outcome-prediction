## Summary

- Archive 3 detailed Phase 01 ROADMAPs (sc2egset, aoe2companion, aoestats) to `archive/ROADMAP_2026-04-09_detailed_phase01.md`
- Write 3 new skeleton ROADMAPs: Phase 01 replaced with generic Pipeline Section list (no Steps, no gate section); Phases 02-07 and Source data sections preserved exactly
- Fix `thesis/WRITING_STATUS.md`: 2 stale `DRAFTABLE` statuses reset to `BLOCKED`; header Phase 01 progress claim removed; game loop derivation note corrected to not assert empirical confirmation
- Bump version `1.0.0` → `1.0.1`

## Test plan

- [x] Each ROADMAP: `grep -c "Phase 01 — Data Exploration (placeholder)"` returns 1
- [x] Each ROADMAP: `grep -c "01_01_01"` returns 0 (no Step-level identifiers in Phase 01)
- [x] Each ROADMAP: `grep -c "Phase 01 gate"` returns 0 (no gate section)
- [x] Each ROADMAP: `grep -c "Phase 02 — Feature Engineering (placeholder)"` returns 1
- [x] Each ROADMAP: `grep -c "Phase 07 — Thesis Writing Wrap-up (gate marker)"` returns 1
- [x] sc2egset ROADMAP: `grep -c "22.4 game loops"` returns 1
- [x] aoe2companion ROADMAP: `grep -c "Raw data is immutable"` returns 1; `grep -c "WARNING — snapshot tables"` returns 1
- [x] aoestats ROADMAP: `grep -c "WARNING — known data gap"` returns 1; `grep -c "WARNING — schema drift"` returns 1
- [x] `thesis/WRITING_STATUS.md`: no section-status rows with `DRAFTABLE`; "not_started" on line 3; "re-derive from data" present; 2 "Blocked until Phase 01" lines present
- [x] `pyproject.toml` shows `version = "1.0.1"`
- [x] No Python source files changed — linting and tests not required for this chore PR

🤖 Generated with [Claude Code](https://claude.com/claude-code)

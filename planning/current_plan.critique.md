# Plan critique — docs/thesis-appendix-key-canonicalization
reviewer: reviewer-adversarial (Category F mandatory pre-execution gate)
base_ref: 637fdb9349d59598f211530ac5a2466d2a83bc69

## Round 1 — reviewer-adversarial plan gate (2026-05-19): APPROVE-WITH-CONDITIONS

Scope verified against live repo @ master 637fdb93 (v3.61.0; PR #225+#226 merged). Canonicalization methodology sound. One load-bearing planner premise (Baek2022 embedded block "byte-equal to canonical") was MISCITED — the block differs from references.bib:25–34 in field order and whitespace — so the Baek edit is re-scoped to KEY-TOKEN-ONLY (no field reorder, no whitespace re-alignment). Open Question 1 (Porcpine embedded reconciliation depth) resolved: reading (b), KEY-SWAP ONLY (references.bib is the single source of truth and is untouched; importing canonical note/url/howpublished would create a re-driftable second copy with zero compiled benefit). Open Question 2 (BT2025Survey) resolved: REPAIR in place, NOT remove, NOT import — arXiv:2601.14727 is a real paper, title matches verbatim, only author (Fang, Shuxing and Han, Ruijian and Luo, Yuanhang and Xu, Yiming) and year (2026) were wrong. Binding conditions: B1 — Baek edit key-only, strike the byte-equality premise; C1 — repair BT2025Survey at all three loci (embedded block + inline "2025 survey" + References-list "Li, Y. et al. (2025)"), half-repair is unsound; C2 — plan and PR body must carry the verbatim Herbrich key-vs-year framing sentence; C3 — reading (b) key-only swap binding for BOTH Porcpine and Baek embedded blocks. NITs (no escalation): plan line offsets drift slightly (re-grep before edit); bundling the #226 INDEX archival is acceptable (docs-only release tail, no empirical lineage); sweep-and-report (not edit) for non-named embedded blocks is the correct conservative posture. references.bib untouched; chapters already use canonical keys (zero live-citation blast radius). Gate: PROCEED to execution with B1/C1/C2/C3 bound.
